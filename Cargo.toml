[package]
name = "daw-ai"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "A local, prompt-driven music studio"
license = "MIT"

[dependencies]
serde_json = "1.0"

[profile.release]
strip = true
lto = true
codegen-units = 1
